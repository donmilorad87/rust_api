FROM provectuslabs/kafka-ui:latest

# Kafka UI runs as non-root user by default
# No additional configuration needed - all settings via environment variables

EXPOSE 8080

# Default entrypoint from base image handles startup
