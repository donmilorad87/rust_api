{
  "name": "blazing-sun-oauth-consent",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build --mode production",
    "build:prod": "vite build --mode production",
    "build:dev": "vite build --mode development",
    "watch": "vite build --watch --mode development"
  },
  "devDependencies": {
    "sass-embedded": "^1.97.3",
    "vite": "^7.3.1"
  }
}
